#!/usr/bin/env bash

# based on https://hexdocs.pm/phoenix/1.5.12/releases.html

set -e

mix do hex.local --force, rebar.local --force

mix deps.get --only prod

export MIX_ENV=prod

mix compile

npm install

elm package install -y

npm run deploy

mix phx.digest

mix release strobe
